<Page x:Class="Hoscy.Ui.Pages.PageOutput"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Hoscy"
      xmlns:ui="clr-namespace:Hoscy.Ui"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageOutput">

    <ScrollViewer Margin="16,8,8,8" VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Textbox -->
            <StackPanel>
                <WrapPanel>
                    <Label Content="Textbox" Foreground="White" FontSize="30" FontFamily="Microsoft YaHei UI" FontWeight="Bold"/>
                    <Button Content="Skip Current Queue" Margin="16,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Click="Button_SkipBox" Width="181"/>
                </WrapPanel>

                <!-- Message Timeout -->
                <StackPanel>
                    <Label Content="Message Timeout" FontSize="24" FontWeight="Bold"/>

                    <StackPanel Margin="8,4,0,0">
                        <CheckBox x:Name="textboxDynamicTimeout" Content="Enable dynamic timeout" IsChecked="{Binding Textbox.DynamicTimeout, Source={x:Static local:Config.Data}}" Click="TextboxDynamicTimeout_Checked"/>

                        <StackPanel x:Name="optionDynamicTimeout" Margin="0,4,0,0">
                            <WrapPanel>
                                <TextBox x:Name="textboxTimeoutMultiplier" VerticalAlignment="Center" Width="120">
                                    <TextBox.Text>
                                        <Binding Source="{x:Static local:Config.Data}" Path="Textbox.TimeoutMultiplier" UpdateSourceTrigger="PropertyChanged">
                                            <Binding.ValidationRules>
                                                <ui:IntegerValidationRule Min="1000" Max="10000"/>
                                            </Binding.ValidationRules>
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>
                                <Label Content ="Dynamic timeout (ms/20char)"/>
                            </WrapPanel>
                            <WrapPanel>
                                <TextBox x:Name="textboxMinimumTimeout" VerticalAlignment="Center" Width="120">
                                    <TextBox.Text>
                                        <Binding Source="{x:Static local:Config.Data}" Path="Textbox.MinimumTimeout" UpdateSourceTrigger="PropertyChanged">
                                            <Binding.ValidationRules>
                                                <ui:IntegerValidationRule Min="1000" Max="30000"/>
                                            </Binding.ValidationRules>
                                        </Binding>
                                    </TextBox.Text>
                                </TextBox>
                                <Label Content ="Minimum timeout (ms)"/>
                            </WrapPanel>
                        </StackPanel>

                        <WrapPanel x:Name="optionDefaultTimeout">
                            <TextBox x:Name="textboxDefaultTimeout" VerticalAlignment="Center" Width="120">
                                <TextBox.Text>
                                    <Binding Source="{x:Static local:Config.Data}" Path="Textbox.DefaultTimeout" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <ui:IntegerValidationRule Min="1000" Max="30000"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <Label Content="Default timeout (ms)"/>
                        </WrapPanel>
                    </StackPanel>

                </StackPanel>

                <!-- Other -->
                <StackPanel Margin="0,16,0,0">
                    <Label Content="Other Options" FontSize="24" FontWeight="Bold"/>

                    
                    <StackPanel Margin="8,4,0,0">
                        <CheckBox x:Name="textboxAutoClearMsg" Content="Clear automatically after messages" IsChecked="{Binding Textbox.AutomaticClearMessage, Source={x:Static local:Config.Data}}"/>
                        <CheckBox x:Name="textboxAutoClearNot" Margin="0,4,0,0" Content="Clear automatically after notifications" IsChecked="{Binding Textbox.AutomaticClearNotification, Source={x:Static local:Config.Data}}"/>
                        <CheckBox x:Name="textboxMediaStatus" Margin="0,4,0,0" Content="Show media status" IsChecked="{Binding Textbox.ShowMediaStatus, Source={x:Static local:Config.Data}}"/>
                        
                        <WrapPanel>
                            <TextBox x:Name="textboxMaxTextlength" VerticalAlignment="Center" Width="120">
                                <TextBox.Text>
                                    <Binding Source="{x:Static local:Config.Data}" Path="Textbox.MaxLength" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <ui:IntegerValidationRule Min="50" Max="130"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <Label Content="Max. text length"/>
                        </WrapPanel>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <!-- STT -->
            <StackPanel Margin="0,32,0,0">
                <WrapPanel>
                    <Label Content="Text to Speech" FontSize="30" FontWeight="Bold"/>
                    <Button Content="Skip Current Speech" Margin="16,0,0,0" Click="Button_SkipSpeech" Width="186" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                </WrapPanel>

                <!-- Audio Options -->
                <StackPanel>
                    <Label Content="Audio Options" FontSize="24" FontWeight="Bold"/>

                    <StackPanel Margin="8,4,0,0">
                        <WrapPanel>
                            <ComboBox x:Name="speechSpeakerBox" Width="240" VerticalAlignment="Center" SelectionChanged="SpeechSpeakerBox_SelectionChanged"/>
                            <Label Content="Output speakers"/>
                        </WrapPanel>

                        <WrapPanel Margin="0,4,0,0">
                            <ComboBox x:Name="speechWindowsSynthBox" Width="240" VerticalAlignment="Center" SelectionChanged="SpeechWindowsSynthBox_SelectionChanged"/>
                            <Label Content="Speech voice"/>
                        </WrapPanel>

                        <WrapPanel Margin="0,4,0,0">
                            <Slider Width="240" VerticalAlignment="Center" Minimum="0" Maximum="1" Value="{Binding Speech.SpeakerVolume, Source={x:Static local:Config.Data}}" ValueChanged="Slider_Volume" LargeChange="0.25" SmallChange="0.05" TickFrequency="0.05" IsSnapToTickEnabled="True" Height="17" FontSize="18" FontFamily="Microsoft YaHei UI"/>
                            <Label Content="Speech volume"/>
                        </WrapPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Other Options -->
                <StackPanel Margin="0,16,0,0">
                    <Label Content="Other Options" FontSize="24" FontWeight="Bold"/>

                    <StackPanel Margin="8,4,0,0">
                        <WrapPanel>
                            <TextBox x:Name="oscOscIp" VerticalAlignment="Center" Width="120">
                                <TextBox.Text>
                                    <Binding Source="{x:Static local:Config.Data}" Path="Speech.MaxLenTtsString" UpdateSourceTrigger="PropertyChanged">
                                        <Binding.ValidationRules>
                                            <ui:IntegerValidationRule Min="1" Max="99999"/>
                                        </Binding.ValidationRules>
                                    </Binding>
                                </TextBox.Text>
                            </TextBox>
                            <Label Content="Max. text length"/>
                        </WrapPanel>
                        <CheckBox x:Name="skipTooLong" Content="Skip too long messages" Margin="0,4,0,0" IsChecked="{Binding Speech.SkipLongerMessages, Source={x:Static local:Config.Data}}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
